# Shell eval fixtures

Used by [`scripts/bench-shells.sh`](../../scripts/bench-shells.sh).

- `fixtures/tiny-crate` — tiny Rust binary for compilation tasks
- `fixtures/search-tree` — generated on first run (2000 text files); gitignored
- `results/` — latest benchmark tables (regenerate with the script)

See [`docs/BENCHMARKS.md`](../../docs/BENCHMARKS.md).
