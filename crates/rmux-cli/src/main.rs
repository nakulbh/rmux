#![forbid(unsafe_code)]
//! CLI tool for controlling a running rmux instance.
//!
//! Communicates with the rmux socket server via JSON-RPC
//! over a Unix domain socket. Accepts subcommands that mirror
//! the cmux CLI where practical.
//!
//! Will be fully implemented in Phase 3.

fn main() {
    println!("rmux-cli: not yet implemented (Phase 3)");
}
