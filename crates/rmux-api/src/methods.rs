//! Method handler registry and dispatch.
//!
//! Maps JSON-RPC method names to handler functions.
//! Supports dynamic registration of methods from different subsystems.
//!
//! Will be fully implemented in Phase 3.

/// Method registry — placeholder for Phase 3.
pub struct MethodRegistry;

impl MethodRegistry {
    /// Create a new, empty method registry.
    #[allow(clippy::new_without_default)]
    pub fn new() -> Self {
        Self
    }
}
